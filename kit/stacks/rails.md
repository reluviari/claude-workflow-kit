# Rails Stack Notes

Look for Gemfile, routes, controllers, models, services, jobs, migrations, schema and tests/specs.

Prefer Rails conventions, thin controllers, explicit service objects when already used, and scoped migrations.
