# Generate Agents From Scope

Create `.claude/agents` based on the project's real stack and scope.

Rules:

- Create only useful agents.
- Adapt each agent to the stack.
- Include responsibility, when to use, files to inspect, expected output, constraints and checklist.
- Do not create generic agents that add no value.
